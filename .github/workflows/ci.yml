name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - '.gitignore'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Reentry.slnx

      - name: Build
        run: dotnet build Reentry.slnx -c Release --no-restore

      # The test project isn't in the slnx (app-only solution), so it restores/builds itself here.
      - name: Test
        run: dotnet test tests/Reentry.Core.Tests/Reentry.Core.Tests.csproj -c Release
